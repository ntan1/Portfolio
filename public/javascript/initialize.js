let wordList = ['Web Developer', 'Metalhead', 'Football Fanatic', 'Dog Lover'];
const wordSpeed = 2000;
const letterSpeed = 100;

let currentWord = wordList[0];
let nextWord = wordList[1];
let wordIndex = 0;
let maxLength = nextWord.length;